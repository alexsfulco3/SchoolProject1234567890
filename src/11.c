#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int age = 25;
    char name[] = "Alice";
    float gpa = 3.8;

    printf("Name: %s\n", name);
    printf("Age: %d\n", age);
    printf("GPA: %.1f\n", gpa);

    return 0;
}
